# Process additional parameters

Ensure ellipsis parameters have the default values This function is used
in two separate workflows, so a single function to edit makes sense.

## Usage

``` r
processEllipsis(input)
```

## Arguments

- input:

  list of ellipsis arguments

## Value

list of arguments adjusted for defaults
