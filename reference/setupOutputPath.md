# Setup Output Path

In the working directory, we need to check if a path to a directory
exists, and if not create it. Ensures smooth running of the automated
functions in TestTrackMateR.

## Usage

``` r
setupOutputPath(fpath)
```

## Arguments

- fpath:

  string containing a path that may or may not exist
