# Merge Data Frames for export

Properties for individual traces (tracks) from different experiments
need to be merged for export. Utility function, not for export.

## Usage

``` r
mergeDataFramesForExport(x, y)
```

## Arguments

- x:

  data frame to be coerced

- y:

  data frame to be coerced
